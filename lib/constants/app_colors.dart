import 'package:flutter/material.dart';

/// Definição centralizada de cores utilizadas na aplicação.
///
/// Esta classe armazena todas as cores do tema da aplicação,
/// permitindo fácil manutenção e reutilização em toda a app.
class AppColors {
  // Cores principais
  static const Color darkNavy = Color(0xFF090129);
  static const Color accentPink = Color(0xFFFF00D4);
  static const Color accentGreen = Color(0xFF99FF33);
  static const Color purplePrimary = Color(0xFF7A00FF);
  
  // Cores claras
  static const Color lightGrey = Color(0xFFEFEFEF);
  static const Color white = Colors.white;
  static const Color black = Colors.black;
  
  // Destaque
  static const Color cyanAccent = Color.fromARGB(255, 0, 247, 255);
  
  AppColors._();
}
